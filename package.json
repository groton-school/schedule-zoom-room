{
  "name": "schedule-zoom-room",
  "version": "1.0.0",
  "scripts": {
    "setup": "./bin/setup.mjs",
    "build": "webpack",
    "deploy": "run-s deploy:*",
    "deploy:build": "npm run build",
    "deploy:publish": "./bin/deploy.mjs",
    "logs": "./bin/logs.mjs",
    "update": "pnpm up; npm up --lock-file-only"
  },
  "author": {
    "name": "Seth Battis",
    "url": "https://github.com/groton-school"
  },
  "license": "GPL-3.0",
  "devDependencies": {
    "@battis/eslint-config": "latest",
    "@battis/partly-gcloudy": "^0.3.0",
    "@battis/prettier-config": "latest",
    "@battis/qui-cli": "^0.3.7",
    "@battis/webpack": "latest",
    "@types/node": "^20.10.3",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.3.2"
  },
  "prettier": "@battis/prettier-config",
  "eslintConfig": {
    "extends": "@battis/eslint-config"
  }
}
