{
  "name": "@groton/schedule-zoom-room",
  "version": "2.0.0",
  "description": "Sync Zoom Room URLs into resource calendars",
  "scripts": {
    "build": "webpack",
    "deploy": "run-s deploy:*",
    "deploy:build": "pnpm run build",
    "deploy:push": "clasp push --force"
  },
  "author": {
    "name": "Seth Battis",
    "url": "https://github.com/groton-school"
  },
  "license": "GPL-3.0",
  "dependencies": {
    "@battis/gas-lighter": "^0.5.1",
    "@battis/google.calendar": "^0.1.4",
    "@types/node": "^20.11.27"
  },
  "devDependencies": {
    "@battis/eslint-config": "latest",
    "@battis/prettier-config": "latest",
    "@google/clasp": "^2.4.2",
    "npm-run-all": "^4.1.5"
  },
  "prettier": "@battis/prettier-config",
  "eslintConfig": {
    "extends": "@battis/eslint-config"
  }
}
